- name: my first playbook
  hosts: servers
  vars:
    movie: "shubham"
  tasks:
    - name: first task
      debug:
        msg: "Hello this is {{ movie }}"
  tasks:
    - name: second task
      debug:
        msg: "hello this is {{ movie }}"
